Vote Total:<%=@answer.total_votes%>

<% if logged_in? && current_user != @answer.user && current_user.has_voted?(@answer) == false%>
  <%= button_to 'Upvote', upvote_question_path(@answer), method: :post%>
  <%= button_to 'Downvote', downvote_question_path(@answer), method: :post%>
<%end%>

